#include <stdio.h>
/**
* main - for
* Return: counting
*/

int main(void)
{
	int i = 0;
	
	for( i == 49 ; i <= 56 ; i++ )
	{
	for( i == 48 ; i <= 57 ; i++ )
	if( i == 49 )
	{
	putchar(i);	
	}
	else if( i == 50 && i = 57 )
	{
	putchar(i);
	}
	else if( i == 51 && i == 48 )
	putchar(i);
	putchar(i);
	}
 	return (0);
}
