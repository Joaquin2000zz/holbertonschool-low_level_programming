#include <stdio.h>
/**
* main - for
* Return: counting
*/

int main(void)
{
	int i;
	int ii;

	for( ii = 48 ; ii <= 56 ; ii++ )
	{
	putchar(ii);
	putchar(i);
	for( i = 49 ; i <= 57 ; i++ )
	{
	if( i != ii )
	{
	}
	putchar(',');
	putchar(' ');
	}
	}
 	return (0);
	}
