#include <stdio.h>
/**
 * * main - for
 * * Return: the alphabet
 * */

int main(void)
{
	int alphabet;

	for (alphabet = 122 ; alphabet <= 97 ; alphabet--)
	putchar(alphabet);
	putchar('\n');

	return (0);
}
