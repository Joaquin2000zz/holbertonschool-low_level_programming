#include <stdio.h>
/**
* main - for
* Return: the alphabet
*/

int main(void)
{
	char alphabet = 'a';

	for (alphabet = 'a' ; alphabet <= 'z' ; alphabet++)
	putchar(alphabet);
	putchar('\n');

	return (0);
}
